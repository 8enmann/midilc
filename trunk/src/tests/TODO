Tests to make:
play:
  single note
  single chord
  single sequence
type checking
adding to sequence
function call
recursion
add
subtract
equality (LRM section 5.4.3)
for
local scope
global scope
if
if else
subscripting
subscripting for assignment, e.g., seq[3] = {A,C,E}
direct selection
casting up
equality
continue
break
return
